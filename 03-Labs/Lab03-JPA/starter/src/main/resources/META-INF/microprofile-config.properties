# MicroProfile Config properties
# Lab 03 - JPA and Database Integration

# Application configuration
app.name=Banking JPA Application
app.version=1.0.0

# Web configuration
web.pagination.default.size=10
feature.client.deletion.enabled=true

# Database configuration (for reference, actual values in bootstrap.properties)
db.host=localhost
db.port=5432
db.name=bankdb
db.user=bankuser

# Made with Bob