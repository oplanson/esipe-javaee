<?xml version="1.0" encoding="UTF-8"?>
<!-- © Copyright 2026 Olivier Planson. All rights reserved. Reproduction prohibited. Made with IBM Bob. -->
<server description="Banking Application Server - Lab 05 REST API">

    <!-- Enable features -->
    <featureManager>
        <!-- Jakarta EE Web Profile (includes CDI, JPA, Servlet, JSP, Transactions, JAX-RS, etc.) -->
        <feature>webProfile-10.0</feature>
        
        <!-- JAX-RS for RESTful Web Services -->
        <feature>restfulWS-3.1</feature>
        
        <!-- JSON-B for JSON binding -->
        <feature>jsonb-3.0</feature>
        
        <!-- JSON-P for JSON processing -->
        <feature>jsonp-2.1</feature>
        
        <!-- Bean Validation for REST validation -->
        <feature>beanValidation-3.0</feature>
        
        <!-- MicroProfile -->
        <feature>mpConfig-3.0</feature>
        <feature>mpHealth-4.0</feature>
        <feature>mpMetrics-5.0</feature>
        <feature>mpOpenAPI-3.1</feature>
        <feature>mpRestClient-3.0</feature>
    </featureManager>

    <!-- HTTP Endpoint -->
    <httpEndpoint id="defaultHttpEndpoint"
                  httpPort="${http.port}"
                  httpsPort="${https.port}"
                  host="*" />
    
    <!-- MicroProfile Metrics Configuration -->
    <!-- Disable authentication for metrics endpoint in development -->
    <mpMetrics authentication="false" />

    <!-- PostgreSQL JDBC Driver -->
    <library id="postgresql-library">
        <fileset dir="${shared.resource.dir}/postgresql" includes="*.jar"/>
    </library>

    <!-- DataSource Configuration -->
    <dataSource id="bankingDS" jndiName="jdbc/bankingDS">
        <jdbcDriver libraryRef="postgresql-library"/>
        <properties.postgresql
            serverName="${db.host}"
            portNumber="${db.port}"
            databaseName="${db.name}"
            user="${db.user}"
            password="${db.password}"
            currentSchema="public"/>
        <connectionManager
            maxPoolSize="10"
            minPoolSize="2"
            connectionTimeout="30s"
            maxIdleTime="5m"
            reapTime="3m"/>
    </dataSource>

    <!-- Application Configuration -->
    <webApplication id="banking-rest-app"
                    location="banking-rest-app.war"
                    contextRoot="/" />

    <!-- Logging Configuration -->
    <logging
        traceSpecification="*=info:com.bank.*=fine"
        maxFileSize="20"
        maxFiles="10"
        consoleLogLevel="INFO"/>

    <!-- Web Container Configuration -->
    <webContainer
        deferServletLoad="false"
        trustHostHeaderPort="true"/>

    <!-- CDI Configuration -->
    <!-- CDI is automatically enabled when cdi-4.0 feature is present -->
    <!-- beans.xml in WEB-INF controls bean discovery mode -->

    <!-- Transaction Manager Configuration -->
    <transaction
        totalTranLifetimeTimeout="120s"
        clientInactivityTimeout="60s"/>

</server>

<!-- Made with Bob -->
