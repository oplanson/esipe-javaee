# Bootstrap properties for Open Liberty server
# Lab 05 - JAX-RS and RESTful Web Services

# HTTP ports
http.port=9080
https.port=9443

# Database connection properties
# Note: db_host should be overridden when running in containers
# Use 'host.containers.internal' for Podman/Docker to access host services
# Use 'localhost' only for local Liberty development
# Liberty automatically converts underscores to dots (db_host -> db.host)
db_host=localhost
db_port=5432
db_name=bankdb
db_user=bankuser
db_password=bankpass

# Application name
app.name=Banking REST Application

# Made with Bob